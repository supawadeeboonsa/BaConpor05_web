# Baipor Portfolio — Next.js

Converted from the uploaded `portfolio.html`.

## Run
```bash
npm install
npm run dev
```

Then open http://localhost:3000

The visual design, colors, typography, sections, embedded images, Figma links, and scroll/reveal behavior are preserved from the HTML version as the starting point.
